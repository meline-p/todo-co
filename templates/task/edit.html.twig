{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        {% include 'task/_form.html.twig' with {'id': null, 'path': 'task_edit'} %}
        <a href="{{ path('task_list') }}" class="btn btn-primary">Retour à la liste des tâches</a>
    </div>
{% endblock %}
